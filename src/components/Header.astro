---
---

<header class="site-header">
  <div class="site-header__inner">
    <a class="logo" href="#top" aria-label="Animation Vocabulary home">
      <span class="logo__mark" aria-hidden="true"></span>
      <span class="logo__word">Animation <span class="logo__dim">Vocabulary</span></span>
    </a>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 50;
    background: color-mix(in srgb, var(--color-canvas-white) 82%, transparent);
    backdrop-filter: saturate(180%) blur(12px);
    border-bottom: 1px solid var(--border-hairline);
    transition:
      background 0.35s ease,
      backdrop-filter 0.35s ease,
      border-color 0.35s ease;
  }
  /* Once the glossary scrolls under the header the chrome melts away and the
     brand pops out of the centred column to the far-left gutter, floating
     over the content. */
  .site-header--pinned {
    background: transparent;
    backdrop-filter: none;
    border-bottom-color: transparent;
  }
  .site-header__inner {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    gap: 24px;
    padding: 12px 24px;
  }
  .logo {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    text-decoration: none;
    color: var(--color-midnight-ink);
    font-weight: 700;
    font-size: 16px;
    letter-spacing: -0.2px;
  }
  /* Float the brand against the very left edge of the viewport, outside the
     centred column and the main container's padding. */
  .site-header--pinned .logo {
    position: fixed;
    top: 13px;
    left: clamp(16px, 2vw, 28px);
    z-index: 60;
    animation: brand-slide-in 0.4s ease;
  }
  @keyframes brand-slide-in {
    from {
      transform: translateX(-12px);
      opacity: 0;
    }
    to {
      transform: translateX(0);
      opacity: 1;
    }
  }
  .logo__mark {
    width: 22px;
    height: 22px;
    border-radius: 7px;
    background: var(--color-midnight-ink);
    position: relative;
  }
  .logo__mark::after {
    content: "";
    position: absolute;
    inset: 6px;
    border-radius: 999px;
    background: var(--color-canvas-white);
    animation: logo-pulse 2.4s ease-in-out infinite;
  }
  @keyframes logo-pulse {
    0%,
    100% {
      transform: scale(0.7);
      opacity: 0.85;
    }
    50% {
      transform: scale(1);
      opacity: 1;
    }
  }
  .logo__dim {
    color: var(--color-muted-ash);
    font-weight: 500;
  }
</style>

<script>
  const header = document.querySelector<HTMLElement>(".site-header");
  const glossary = document.getElementById("glossary");

  let ticking = false;
  const update = () => {
    const trigger = glossary ? glossary.offsetTop - 64 : 200;
    header?.classList.toggle("site-header--pinned", window.scrollY >= trigger);
    ticking = false;
  };
  window.addEventListener(
    "scroll",
    () => {
      if (ticking) return;
      ticking = true;
      requestAnimationFrame(update);
    },
    { passive: true }
  );
  window.addEventListener("resize", update, { passive: true });
  update();
</script>
